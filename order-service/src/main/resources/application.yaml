spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/order-service-db
    username: postgres
    password: umut123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081
  servlet:
    context-path: /api/v1